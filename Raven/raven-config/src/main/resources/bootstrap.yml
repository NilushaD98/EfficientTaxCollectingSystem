server:
  port : 9999
spring:
  application:
    name : raven-config
  profiles:
    active: native
  cloud:
    config:
      server:
        git:
          uri: https://github.com/NilushaD98/taxconfig.git
  devtools:
    add-properties: false

